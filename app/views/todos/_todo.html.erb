<div id="<%= dom_id todo %>">
  <p>
    <strong>Description:</strong>
    <%= todo.description %>
  </p>

  <%= button_to (todo.high_priority? ? "⭐" : "☆"), toggle_high_priority_todo_path(todo), method: :patch %>
</div>
